/**
 * v0.44.3 — Global compile-time constants injected by vite's `define`
 * (see vite.config.ts). These are string literals in the built bundle,
 * so they are guaranteed to match the actual package.json version at build.
 */
declare const __APP_VERSION__: string;
declare const __APP_BUILD_TIME__: string;
